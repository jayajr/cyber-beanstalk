<%= form_with(model: secret, local: true) do |form| %>
  <% if secret.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(secret.errors.count, "error") %> prohibited this secret from being saved:</h2>

      <ul>
      <% secret.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :uuid %>
    <%= form.text_field :uuid, id: :secret_uuid %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :secret_name %>
  </div>

  <div class="field">
    <%= form.label :secret %>
    <%= form.text_area :secret, id: :secret_secret %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
